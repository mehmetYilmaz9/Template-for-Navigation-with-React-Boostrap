import React from 'react';

export const Header = () => (
    <header className="header">
        <div class="header__text-box">
            <h1 class="heading-primary">
                <span class="heading-primary--main">Template React-Bootstrap</span>
                <span class="heading-primary--sub">With Styled Navigation</span>
            </h1>
        </div>
    </header>

   

);

export default Header;